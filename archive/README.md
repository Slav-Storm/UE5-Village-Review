# Archived milestones

No previous review set existed in this repository at initialization.

Before replacing `latest/` at a major milestone, copy the previous images, manifest and previous `REVIEW.md` into `YYYY-MM-DD_HHMMZ_short-milestone/`. Keep its review links valid (retain a nested `latest/` beside its `REVIEW.md`). Archive selected meaningful milestones, not every tiny iteration. Git history also retains earlier commits.
